# دليل تغليف البرنامج وتوزيعه على العملاء (خطوة بخطوة)

هذا الدليل يشرح كيف تحوّل البرنامج من "ملفات Python تشغّلها يدويًا بأمر
`python app.py`" إلى ملف تنصيب واحد (Setup.exe) ترسله للعميل برابط
وينصّبه بضغطة وحدة، مع بقاء نظام الترخيص (Hardware ID + موافقتك) شغّال
بالضبط متل ما هو.

⚠️ كل الخطوات بالأسفل تُنفَّذ **بجهاز ويندوز عندك أنت** (مو من هذا
المحادثة) — لأن الأدوات المستخدمة (PyInstaller، Inno Setup) تبني ملف
`.exe` مخصص لنفس نظام التشغيل اللي تبنيه فيه بالضبط.

---

## الخطوة 1: تجهيز مجلد نظيف للبرنامج

اسوي نسخة من مجلد البرنامج بمكان جديد (مثلاً `C:\lis-build`) تحتوي:
- `app.py`, `database.py`, وكل ملفات `.py` الثانية (license_manager.py،
  auto_updater.py، daily_counter.py، translations.py، barcode_gen.py،
  astm_host.py...)
- مجلد `templates/` كامل (كل صفحات HTML بما فيهم اللي عدّلناهم سوا)
- مجلد `static/` (الشعارات، الصور الافتراضية، أي CSS/JS منفصل)
- `launcher.py` (الملف اللي جهّزته لك بهذا الرد — يشغّل السيرفر ويفتح
  نافذة شبيهة ببرنامج مستقل تلقائيًا، بدل ما تحتاج تفتح تيرمنال وتكتب
  أوامر يدويًا)
- ملف أيقونة `app_icon.ico` (حوّل شعار المختبر لصيغة .ico من أي موقع
  تحويل صور مجاني إذا ما عندك واحد جاهز)

**لا تنسخ** مجلد `lis.db` القديم اللي فيه بيانات مرضى حقيقية — خلي
البرنامج المُغلَّف يبدأ بقاعدة بيانات فارغة (البرنامج يسويها تلقائيًا
أول تشغيل عبر `init_db()`).

---

## الخطوة 2: تنصيب الأدوات المطلوبة (مرة وحدة بس)

بجهاز الويندوز عندك، افتح Command Prompt وسوّي:

```
pip install pyinstaller
```

نزّل وثبّت [Inno Setup](https://jrsoftware.org/isinfo.php) (مجاني تمامًا،
يُستخدم لبناء ملف التنصيب النهائي).

---

## الخطوة 3: بناء ملف .exe واحد بـPyInstaller

من داخل مجلد `C:\lis-build`:

```
pyinstaller --onefile --noconsole --name "SpecializedHematologistLab" ^
    --add-data "templates;templates" ^
    --add-data "static;static" ^
    --icon "app_icon.ico" ^
    launcher.py
```

لو عندك أي مجلد بيانات ثابت ثاني يستخدمه البرنامج (خطوط، قوالب PDF
جاهزة...)، أضفه بنفس الأسلوب: `--add-data "اسم_المجلد;اسم_المجلد"`.

بعد ما يخلص (يأخذ دقيقة أو دقيقتين)، تلاقي الملف الجاهز بـ:
```
C:\lis-build\dist\SpecializedHematologistLab.exe
```

جرّبه بنفسك أول: دبل كلك عليه، لازم تفتح نفس نافذة البرنامج اللي بالصور
تلقائيًا بدون ما تفتح أي تيرمنال.

---

## الخطوة 4: بناء ملف تنصيب Setup.exe بـInno Setup

افتح Inno Setup، اختر "Create a new script file using the Script
Wizard"، وحدد:
- اسم البرنامج: Specialized Hematologist Lab
- الملف الرئيسي: `SpecializedHematologistLab.exe` (من مجلد `dist`)
- فعّل خيار "Create a desktop icon" حتى ينسوي اختصار سطح مكتب تلقائيًا
  عند العميل بنفس الأيقونة

بعد ما يخلص المعالج، اضغط Build → Compile. بيطلعلك ملف وحيد اسمه
شي متل `SpecializedHematologistLab_Setup.exe` — **هذا هو الملف اللي
ترسله للعميل**.

---

## الخطوة 5: رفع الملف ومشاركة الرابط

ملف الـSetup غالبًا حجمه 80-150 ميغابايت (لأنه يحمل Python كامل جواه) —
واتساب ما يقبل إرسال ملفات بهذا الحجم مباشرة. الحل:
1. ارفع `SpecializedHematologistLab_Setup.exe` على Google Drive أو
   OneDrive (أو استضافة خاصة بيك لو عندك موقع).
2. خلي رابط المشاركة "أي شخص عنده الرابط يقدر يحمّل".
3. انسخ هذا الرابط وأرسله للعميل بالواتساب.

العميل يفتح الرابط → يحمّل → يشغّل Setup.exe → البرنامج ينصّب نفسه
وينسوي اختصار على سطح المكتب.

---

## الخطوة 6: التفعيل يبقى بيدك بالضبط (زي ما هو الآن)

أول ما العميل يفتح البرنامج المُنصَّب، شاشة الترخيص تطلعله معرّف الجهاز
(Hardware ID) الخاص بحاسبته. هو يرسلّك هذا المعرّف بالواتساب، وانت تولّد
كود تفعيل خاص بذلك الجهاز بس من لوحة المصمم (نفس الآلية المبنية أصلاً —
ما تغيّر شي هنا).

---

## بعد أول توزيع: التحديثات

بما إن البرنامج فيه نظام تحديث تلقائي (`auto_updater.py`)، أي تعديل
جديد تسويه بعدين ينوصل للعميل تلقائيًا (إذا مفعّل الاتصال بالإنترنت من
لوحة المصمم) بدون الحاجة تعيد كل خطوات التغليف والتنصيب من الأول — هذا
فقط لأول توزيع للبرنامج على جهاز العميل.
